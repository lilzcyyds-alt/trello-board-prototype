# 2.8 Drag & Drop — Lists (Reorder Columns)

## Summary
Enable dragging list columns to reorder horizontally.

## Requirements
- Dragging a list column:
  - Reorder columns in the horizontal layout
- Visual feedback:
  - Clear drag handle area (optional)
  - Drag overlay preview (optional)

## Acceptance Criteria
- Reordering updates `listOrder` and re-renders correctly
- Does not break card drag-and-drop inside columns

## Implementation Notes (later)
- Use `dnd-kit` Sortable for lists with `horizontalListSortingStrategy`
- Use id scheme, e.g., `list:<listId>`
